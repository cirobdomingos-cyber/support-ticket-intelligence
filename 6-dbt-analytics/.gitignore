# dbt build artifacts — regenerated by `dbt run`, never commit
target/
dbt_packages/

# DuckDB local database files — data, not code
*.duckdb
*.duckdb.wal

# Environment
.env
.venv/
__pycache__/

# Sensitive — override intentionally if credentials-free (like this project)
# profiles.yml

# OS artifacts
.DS_Store
Thumbs.db

# dbt logs
logs/
